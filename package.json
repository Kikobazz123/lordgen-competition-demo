{
  "name": "lordgen-handover-generator",
  "version": "0.1.0",
  "private": true,
  "description": "LordGen client-document generator (LORDGEN_HANDOVER_GENERATOR_BUILD.md). Schema validation, gates, section builders, and HTML template are dependency-free by design; Playwright is the sole dependency, used only for local watermark-free PDF rendering (tools/render_pdf.js).",
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "playwright": "^1.48.0"
  }
}
