# Competition Demo Pre-Flight Report

Generated by the `competition-demo` skill (Phase 9). Structured source: `data/competition-demo-report.json` (gitignored, per-run artifact). Run date: 2026-08-19.

## Results

| # | Check | Status | Evidence |
|---|---|---|---|
| 1 | Demo path | **pass** | `data/research.json` (14.5KB) → `data/score.json` (7.2KB, `top_opportunity` present) → `data/proposal.json`/`proposal.md` (opportunity+score match `score.json`) → `data/execution-plan.json`/`execution-plan.md` → `workflows/competition-demo.json` (matches live n8n) → 2 ClickUp handoff tasks (confirmed live via `clickup_filter_tasks`). Chain intact end to end. |
| 2 | Selected business | **pass** | Internal pipeline artifacts (`research.json`, `proposal.json`, `execution-plan.md`) consistently use "Nick's Plumbing & Air Conditioning." `website/`'s public demo intentionally uses a different, illustrative name ("Ridgeline Plumbing & Air") — a documented 2026-08-18 decision (`website/README.md`, `docs/architecture.md`), not an inconsistency. |
| 3 | All outputs exist | **pass, with a note** | All 5 `data/*.json` files present locally. `proposal.md`/`execution-plan.md` exist locally but are **gitignored — not in the public GitHub repo** (protects private business research, per `docs/architecture.md`'s exclusion policy). A judge browsing only GitHub will not find these two documents; they must be shown live (see `docs/demo-script.md`). Workflow diagram and slide deck (named in `LORDGEN_competition_demo.md`'s deliverables list, not in Skill 07's own spec) are **not yet built** — flagged as an open item, not silently created. |
| 4 | The workflow | **pass** | `workflows/competition-demo.json` is valid JSON (20 nodes), matches the live n8n workflow (`get_workflow_details`), `active: false` (correct — unpublished pending human review, CLAUDE.md §10). `workflow-qa` gaps #1–2 fixed and live-verified 2026-08-19; gap #4 (stray test rows in the live Data Table) is **still open — needs manual cleanup in the n8n UI before the demo**, no MCP tool exists to do it automatically. |
| 5 | The execution plan | **pass, one staleness bug found and fixed** | All `docs/execution-plan-template.md` sections populated. Found: `execution-plan.md`'s Integrations table and Deployment section still said n8n was "Not connected... Phase 6 dependency," even though `data/execution-plan.json`'s structured version already correctly said "connected" and the workflow has long since been built/tested/QA'd — the markdown twin had drifted from its own JSON source. Corrected both sections this run. |
| 6 | The GitHub repository | **pass** | `gh repo view` confirms public visibility. Working tree clean pre-run; `.gitignore` correctly excludes `docs/demo-business.md`, `/proposal.md`, `/execution-plan.md`, and `data/*` (except `data/professions/`). Recent commit history reflects actual current state. |
| 7 | No secrets exposed | **pass** | Tracked-tree scan (`git grep`, credential-pattern regex) found only empty `.env.example`-style placeholders inside `CLAUDE_CODE_UPGRADE_INSTRUCTIONS.md` (e.g. `AI_API_KEY=`) — a documentation template, not a real value. Full-history scan (`git log -p --all`) found nothing beyond routine git author email lines (expected, not a leak). `.env` confirmed untracked and properly matched by `.gitignore` (`git check-ignore -v`). |

## Then: demonstration sequence

Written to `docs/demo-script.md` — the 8-step Core Demo Flow against this build's actual artifacts, plus a "known state going into the demo" section covering the open items above so they're never a surprise mid-demo.

## Summary

6 of 7 checks pass cleanly; check 5 surfaced and fixed a real staleness bug. No blocking issues for a demo. Two things need action before going live:
1. **You**: delete the 5 tagged `QA-TEST-*` rows from the live n8n Data Table (no MCP tool can do this).
2. **Decision needed**: whether a workflow diagram and/or slide deck should be built — currently absent, not assumed in scope for this skill.
