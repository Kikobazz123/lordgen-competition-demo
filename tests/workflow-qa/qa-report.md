# Workflow QA Report

Generated by the `workflow-qa` skill. Structured source: `data/qa-report.json` (gitignored, per-run artifact — regenerate by re-running the skill).

**QA date:** 2026-08-17
**Tested:** `workflows/competition-demo.json` (n8n workflow `055TNXGtfItIgqf1`, unpublished) against `data/execution-plan.json`'s `testing_plan[]` and CLAUDE.md §15's 7 required categories.
**n8n MCP reachable this run:** No — the claude.ai n8n connector was disconnected for this session. All 7 categories were checked by static inspection of the workflow JSON only; no `test_workflow` (pin-data) calls were made. Live-verification fixtures are prepared and ready under `fixtures/` for the next run once the connector reconnects.

## Results

| Category | Method | Status | Summary |
|---|---|---|---|
| Happy path | static | not verified (MCP unreachable) | Control-flow wiring end-to-end matches `workflow_steps` 1–6 exactly. Full behavioral confirmation needs a live run. Separate finding: the log record doesn't capture *who* approved (see Known Gaps). |
| Missing input | static | not verified (MCP unreachable) | The three-field `notEmpty` check and its failure branch are wired correctly. Live confirmation not run. |
| External failure | static | **fail** | No error-output routing exists from either Gmail node to a log/alert node — the "delivery channel unavailable" case isn't implemented. |
| AI failure | static | not verified (MCP unreachable) | The AI-output validity check and its failure branch are wired correctly. Live confirmation not run. |
| Duplicate execution | static (never live-tested, by design) | not verified (MCP unreachable) | The dedup node sits in the right place with no bypass, but its exact operation semantics (`rowNotExists`) aren't confirmed against n8n's real node schema this session. |
| Approval gate | static | **pass** | `Send Summary To Customer` has exactly one inbound edge, gated behind `Dispatcher Approved?`'s true branch — a structural guarantee, not just a runtime observation. |
| Configuration | static | **pass** | All three external-service nodes reference a credential (placeholder, not a real secret); "fails loudly" relies on n8n's own platform behavior for a missing credential, not a purpose-built check node in the workflow. |

## Known gaps

1. **External failure isn't handled.** Neither Gmail node routes a failure anywhere — no logging, no alert. This is a real gap in the workflow as built, not a QA artifact. Fixing it is a separate, human-approved change to `automation-builder`'s output; this skill does not edit the workflow itself.
2. **Approver identity isn't logged.** `execution-plan.json`'s `outputs` calls for logging "who approved it," but `Log: Approved & Sent` only writes a generic notes string, not an approver identifier.
3. **Live verification is incomplete this run.** `test_workflow` + pin data would give a much stronger confirmation than static analysis alone for Happy path, Missing input, AI failure, and Duplicate execution. Re-run this skill once n8n MCP is reachable again — fixtures are already prepared in `fixtures/`.
4. **`rowNotExists` semantics unconfirmed.** The dedup node's operation name is plausible but wasn't checked against n8n's actual Data Table node schema this session.

## Fixtures prepared for the next live run

- `fixtures/happy-path.json` — valid estimate, approved → expect `Log: Approved & Sent`
- `fixtures/missing-input.json` — blank `customerContact` → expect `End (Missing Input)`
- `fixtures/ai-failure.json` — empty AI output → expect `End (AI Failed)`
- `fixtures/approval-rejected.json` — dispatcher rejects → expect `End (Rejected)`

No fixture exists for External failure (static-only category; pin data can't exercise a real delivery failure) or Duplicate execution (intentionally never live-tested — see the skill's hard rules and `data/qa-report.json`).
