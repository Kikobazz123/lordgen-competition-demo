{
  "_status": "live-verified 2026-08-18, execution id 9 -- lastNodeExecuted 'Log: Approved & Sent', row id 5 written to the live Data Table",
  "category": "Happy path",
  "pin_data": {
    "Estimate Submitted (Webhook)": [
      { "json": { "body": { "jobId": "QA-TEST-HAPPY-01", "customerContact": "qa.customer@example.com", "scope": "Replace kitchen sink shutoff valve", "price": "$180-$220" } } }
    ],
    "Draft Estimate Summary": [
      { "json": { "output": "Your estimate for job QA-TEST-HAPPY-01: we'll replace the kitchen sink shutoff valve. Estimated price range is $180-$220." } }
    ],
    "Request Dispatcher Approval": [
      { "json": { "data": { "approved": true } } }
    ],
    "Send Summary To Customer": [
      { "json": { "id": "qa-test-message-id", "threadId": "qa-test-thread-id" } }
    ]
  },
  "expected_terminal_node": "Log: Approved & Sent",
  "_note": "Send Summary To Customer must be pinned explicitly -- it has no credential attached, so test_workflow's documented auto-pin behavior for credentialed nodes did not apply; the first run without this pin errored with 'Node does not have any credentials set'."
}
